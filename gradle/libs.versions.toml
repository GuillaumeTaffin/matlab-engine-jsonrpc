[versions]
junit-jupiter-engine = "5.10.1"

[libraries]
junit-jupiter-engine = { module = "org.junit.jupiter:junit-jupiter-engine", version.ref = "junit-jupiter-engine" }
jsonrpc4j = { module = "com.github.briandilley.jsonrpc4j:jsonrpc4j", version = "1.6" }
javax-servlet = { module = "javax.servlet:javax.servlet-api", version = "4.0.1" }

[plugins]
jvm = { id = "org.jetbrains.kotlin.jvm", version = "1.9.22" }
